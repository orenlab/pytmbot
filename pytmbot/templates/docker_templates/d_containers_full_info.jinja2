{{ thought_balloon }} <b>Container Details:</b>

{{ basket }} <b>Name:</b> <code>{{ container_name }}</code>

{{ flag_in_hole }} <b>CPU Stats:</b>
<code>Periods:</code> {{ container_cpu_stats['periods'] | default('N/A') }}
<code>Throttled:</code> {{ container_cpu_stats['throttled_periods'] | default('N/A') }}
<code>Throttling:</code> {{ container_cpu_stats['throttling_data'] | default('N/A') }}

{{ railway_car }} <b>Memory Stats:</b>
<code>Usage:</code> {{ container_memory_stats['mem_usage'] | default('N/A') }} ({{ container_memory_stats['mem_percent'] | default('N/A') }}%)
<code>Limit:</code> {{ container_memory_stats['mem_limit'] | default('N/A') }}

{{ radio }} <b>Network Stats:</b>
<code>RX/TX bytes:</code> {{ container_network_stats['rx_bytes'] | default('N/A') }}/{{ container_network_stats['tx_bytes'] | default('N/A') }}
<code>RX/TX dropped:</code> {{ container_network_stats['rx_dropped'] | default('N/A') }}/{{ container_network_stats['tx_dropped'] | default('N/A') }}
<code>RX/TX errors:</code> {{ container_network_stats['rx_errors'] | default('N/A') }}/{{ container_network_stats['tx_errors'] | default('N/A') }}

{{ puzzle_piece }} <b>Container State:</b>
<code>Status:</code> {% if container_attrs['running'] %}Running{% elif container_attrs['paused'] %}Paused{% elif container_attrs['restarting'] %}Restarting{% elif container_attrs['dead'] %}Dead{% else %}Unknown
{% endif %}

<code>Restart count:</code> {{ container_attrs['restarting_count'] | default('N/A') }}
<code>Exit code:</code> {{ container_attrs['exit_code'] | default('N/A') }}

{{ safety_pin }} <b>Environment:</b>
<pre>{% if container_attrs['env'] %}{% for value in container_attrs['env'] %}
{{ value }}{% endfor %}{% else %}No environment variables{% endif %}</pre>

{{ sandwich }} <b>Launch Configuration:</b>
<pre>{% if container_attrs['command'] and container_attrs['command']|length > 0 %}Command: {% for value in container_attrs['command'] %}{{ value }} {% endfor %}{% else %}No command specified{% endif %}

{% if container_attrs['args'] and container_attrs['args']|length > 0 %}Arguments: {% for value in container_attrs['args'] %}{{ value }} {% endfor %}{% else %}No arguments specified{% endif %}</pre>

{{ banjo }} <i>End of container details</i>