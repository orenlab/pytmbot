{# d_logs.jinja2 — rich HTML #}
<p><b>{{ emojis['thought_balloon'] }} Container logs</b></p>

<pre><code>{{ logs|e }}</code></pre>
<blockquote>Sensitive values may be masked here. If you need the raw logs, check them on the host with:</blockquote>
<pre><code>sudo docker logs {{ container_name|e }}</code></pre>
