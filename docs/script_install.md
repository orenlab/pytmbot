# Installation Script (`tools/install.sh`) Deprecated

`tools/install.sh` was removed from the repository and is no longer supported.

pyTMBot is designed to run with Docker, and the only supported installation method is Docker / Docker Compose.

See:

- [docker.md](docker.md)
- Docker Hub image: https://hub.docker.com/r/orenlab/pytmbot
