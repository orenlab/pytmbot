{%- set example_apps = [
    'Yandex Key',
    'Microsoft Authenticator',
    'Google Authenticator'
] -%}

{{ thought_balloon }} Hello, {{ name }}.

{{ double_exclamation_mark }} Two-factor authentication (2FA) is required to open this section.
{%- if not has_installed_app %}
Install any app that supports standard TOTP codes if you do not have one yet.
{%- endif %}

<blockquote expandable>
Examples of compatible apps:
{% for app in example_apps -%}
- {{ app }}
{% endfor %}
</blockquote>

{{ first_quarter_moon }} Need to set up 2FA? Tap "Get QR Code".
{{ fountain_pen }} Already set up? Tap "Enter 2FA Code".
