# Keep automatic SonarCloud analysis focused on application code and
# explicitly classify the test suite as tests so PR quality metrics are not
# skewed by pytest-only patterns.
sonar.sources=pytmbot
sonar.tests=tests
sonar.sourceEncoding=UTF-8
sonar.python.version=3.12,3.13,3.14
