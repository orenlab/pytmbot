{{ thought_balloon }} Hello, {{ first_name }}.

Available plugins:

{% for plugin_name, plugin_description in plugins.items() %}- *{{ plugin_name }}*: {{ plugin_description }}
{% endfor %}

Select a plugin below to open it.
